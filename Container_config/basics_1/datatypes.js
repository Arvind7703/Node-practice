"use strict"; // new version syntax of js

//Primitive or value type 

let name = "Arvind"; // String
let age = 20;// Number
let isLogggedin = true // Boolean
let b = null // standalone value (Empty)
let a; //undefined

// Non-primitive reference type

let arr = [1,2,3,4] // Array
let obj = {
    name:"Arvind",
    age:20,             //Object
    isStudent: true,
}

console.log(typeof a) // type of is to check the data type